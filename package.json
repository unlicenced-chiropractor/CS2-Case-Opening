{
  "name": "casestrike",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build && wrangler dev",
    "build": "vite build",
    "preview": "vite preview",
    "api:dev": "wrangler dev",
    "cf:dev": "npm run dev",
    "cf:deploy": "npm run build && wrangler deploy",
    "db:migrate:local": "wrangler d1 execute DB --local --file=./worker/schema.sql",
    "db:migrate:remote": "wrangler d1 execute DB --remote --file=./worker/schema.sql",
    "db:migrate:m1": "wrangler d1 execute DB --local --file=./worker/migrations/0001_add_sold_at.sql && wrangler d1 execute DB --local --file=./worker/migrations/0002_add_is_admin.sql && wrangler d1 execute DB --local --file=./worker/migrations/0003_add_password_reset_tokens.sql && wrangler d1 execute DB --local --file=./worker/migrations/0004_add_last_login_at.sql",
    "db:migrate:m2": "wrangler d1 execute DB --remote --file=./worker/migrations/0001_add_sold_at.sql && wrangler d1 execute DB --remote --file=./worker/migrations/0002_add_is_admin.sql && wrangler d1 execute DB --remote --file=./worker/migrations/0003_add_password_reset_tokens.sql && wrangler d1 execute DB --remote --file=./worker/migrations/0004_add_last_login_at.sql"
  },
  "dependencies": {
    "vue": "^3.5.32",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "@vitejs/plugin-vue": "^6.0.5",
    "tailwindcss": "^4.1.14",
    "vite": "^8.0.4",
    "wrangler": "^4.42.3",
    "@cloudflare/workers-types": "^4.20250407.0"
  }
}
